import React {{#if (isTypescript extension)}}, { ReactNode } {{/if}}from 'react'

{{#if (isTypescript extension)}}
interface {{ name }}Props {
    children: ReactNode;
    {{#each props }}
    {{ this }}: any;
    {{/each}}
}
{{/if}}

const {{name}}
{{#if (isTypescript extension)}}: React.FC<{{ name }}Props>{{/if}}
= ({ children,
{{#if props}}{{#each props }}{{ this }},{{/each}}{{/if}}
}) => {
    return (
        <div>{children}</div>
    )
}

export default {{name}}
